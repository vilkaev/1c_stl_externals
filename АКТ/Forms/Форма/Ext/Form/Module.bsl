
&НаСервере
Функция НапечататьНаСервере()
	МассивОбъектов=новый Массив;
	МассивОбъектов.Добавить(Объект.Док);
	ОбработкаОбъект = РеквизитФормыВЗначение("Объект"); 
	ТабДок=ОбработкаОбъект.ПечатьВнешнейПечатнойФормы(МассивОбъектов,  Новый СписокЗначений );
	Возврат ТабДок;
КонецФункции // ЗапуститьНаСервере()
 
&НаКлиенте
Процедура Напечатать(Команда)
	ТабДок = НапечататьНаСервере();
	ТабДок.Показать();
КонецПроцедуры
